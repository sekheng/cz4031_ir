# OS=Windows

cd <path/to/deployment/solr-8.11.2>

# start solr in stand alone mode
bin\solr.cmd start

# create a core for the stock project
bin\solr.cmd create_core -c stock_project_core -d stock_project_configs

# launch python api and follow its instructions to perform add index, remove index, query index
cd python_api
jupyter notebook

# terminate solr after usage or schema updates
bin\solr.cmd stop -all